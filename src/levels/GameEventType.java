package levels;

/*
 * Different events we need to log for animation
 */
public enum GameEventType {
	CREEP_SPAWNED, CREEP_KILLED, CREEP_ESCAPED, PROJECTILE_FIRED, PROJECTILE_EXPIRED, SIPHON_CHANGED, TOWER_CREATED, TOWER_DESTROYED;
}
